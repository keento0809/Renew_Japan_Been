<% if false %>
個別にJSを読み込む。読み込ませたいlogin.jsのパスはapp/javascript/packsなので
javascript_pack_tag 'login'はapp/javascript/packs/login.jsを読み込むという意味
<% end %>
<head>
<%# <%= javascript_pack_tag "login" %>
</head>

<div class="login-body" id='slideshow'>
<div class="bg-layer">
<h2>Japan been</h2>

<section class="login-main">
  <% if false %>
  <p>You need to login.</p>

  <%= link_to "LOGIN", login_path %>
  <% end %>

  <%# if false %>
  <%= form_with(url: login_path, scope: :session, local: true) do |f| %>
        <section class="u-form">
            <%= f.email_field :email, placeholder:"Email", class: 'form-control' %>
            <i class="fas fa-user"></i>
        </section>
        <section class="u-form">
            <%= f.password_field :password, placeholder:"Password", class: 'form-control' %>
            <i class="fas fa-lock"></i>
        </section>
        <section class="u-form">
            <%= f.label :remember_me do %>
                <%= f.check_box :remember_me, id: 'not-checkbox' %>
                <span>REMEMBER ME</span>
            <% end %>
        </section>
        <%= f.submit "LOGIN", class: 'login-btn' %>
    <% end %>
    <%# end %>

        <section class="create-account">
        <span>New User? Sign up here!</span>

        <%= link_to "CREATE ACCOUNT", signup_path %>
      </section>
</section>
</div>
    <%= javascript_pack_tag "login" %>
</div>

